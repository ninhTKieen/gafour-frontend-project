import React from 'react';

import './LoginSidebar.css';

const LoginSideBar: React.FC = () => {
  return (
    <React.Fragment>
      <div className="login-side-bar">
        <div className="login-side-bar-web-title">
          Gafour
        </div>
      </div>
    </React.Fragment>
  );
};

export default LoginSideBar;
